global:
  resolve_timeout: 1m

route:
  receiver: default
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: default
    webhook_configs:
      - url: 'http://localhost:5001'
        send_resolved: true
